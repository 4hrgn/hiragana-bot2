name: Hiragana Tweet Bot

on:
  schedule:
    - cron: '0 9 * * *'  # 毎日 9:00（UTC）に実行（日本時間18:00）
  workflow_dispatch:      # 手動実行もOK

jobs:
  tweet:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          pip install -r requirements.txt

      - name: Run bot script
        env:
          API_KEY: ${{ tGvzTQUEKswuVCkJn5zKoudc2 }}
          API_SECRET: ${{ SM1K571d8KwiCyWECPsSVPtbsIweENldvCrnF7mGJmFEIpGPYS }}
          ACCESS_TOKEN: ${{ 1899010131423289344-DNuhEryB4ulVUDEnhbjKfrknAesykk }}
          ACCESS_TOKEN_SECRET: ${{ v9pwyqDzHamtYHOVesYoQW9yso3j7GtuvOl9hCCtREijp }}
        run: python bot.py
